// Script-tag embed. Auto-init (data-site / window.PulseConfig) lands in PULSE-390;
// for now this exposes the same surface as the SDK on window.Pulse.
export { Pulse } from '../index'
export type { PulseInstance, PulseConfig, SubmitResult } from '../index'
