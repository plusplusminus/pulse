# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Encryption Key
# Generate a random 256-bit (32-byte) base64 key for encrypting stored API tokens
# You can generate one with: openssl rand -base64 32
ENCRYPTION_KEY=your-base64-encryption-key-here

# WorkOS AuthKit (consumed by @workos-inc/authkit-nextjs, not the Node SDK)
# Disable PKCE — this is a server-side app with client_secret, and PKCE's 10-minute
# cookie TTL breaks magic link flows where users take longer to check email.
WORKOS_DISABLE_PKCE=true

# App URL
# Full URL of your deployment (used for webhooks, email links, etc.)
NEXT_PUBLIC_APP_URL=https://pulse.plusplusminus.co.za

# Resend (email delivery)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
FROM_EMAIL=notifications@yourdomain.com

# Cron job authentication
# Generate a random secret: openssl rand -hex 32
# Must match CRON_SECRET in Vercel project settings
CRON_SECRET=your-cron-secret-here

# PostHog Analytics
# Get these from your PostHog project settings: https://app.posthog.com/settings/project
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
NEXT_PUBLIC_POSTHOG_KEY=phc_your_project_key_here

# Sentry Error Tracking
# Get your DSN from https://sentry.io → Project Settings → Client Keys
NEXT_PUBLIC_SENTRY_DSN=https://your-dsn@o123.ingest.sentry.io/456
SENTRY_DSN=https://your-dsn@o123.ingest.sentry.io/456
# Auth token for source map uploads (create at https://sentry.io/settings/auth-tokens/)
# Needs project:releases and org:read scopes
# SENTRY_AUTH_TOKEN=sntrys_your_auth_token_here
# SENTRY_ORG=your-sentry-org
# SENTRY_PROJECT=your-sentry-project
